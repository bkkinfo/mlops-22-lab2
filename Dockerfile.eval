FROM python:3.8.1

COPY ./*.py /exp/
COPY ./requirements.txt /exp/requirements.txt

RUN pip3 install --no-cache-dir -r /exp/requirements.txt

WORKDIR /exp

ENV CLF_NAME="svm"
ENV RANDOM_STATE="15"

CMD python plot_digits_classification.py --clf_name ${CLF_NAME} --random_state ${RANDOM_STATE}